__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/
.taskuary/
*.db
.env
website/node_modules/
website/ui.png
.codex-tmp/
site/taskuary/

# scratch screenshots dropped in the repo root
/*.png

# the WhatsApp bridge's own install and pairing state - never committed
taskuary/whatsapp/node_modules/
taskuary/whatsapp/wa-auth/
taskuary/whatsapp/wa-media/
taskuary/whatsapp/wa-bridge.log
taskuary/whatsapp/package-lock.json

# editor droppings and scratch files - a vim swap once leaked a username into the repo
*.swp
*.swo
*.tmp

# hero-GIF capture scratch (frames are regenerated; only docs/hero.gif is committed)
website/_hero/
website/_gifcheck.png
website/_sheet.png

# the triage dataset built from YOUR mail (taskuary --evalset share): review it, then `git add -f` on purpose
tests/data/triage_cases.jsonl

# one-off screenshot driver, rewritten per capture
website/shot_tmp.mjs
# the published design canvas is a 2.5MB generated bundle - build.mjs and the artboards are the source
docs/design/**/*-and-stage.html
# a seeded design canvas is a 2.5MB generated bundle - the .dc.html artboards are the source
docs/design/**/timeline-*.html
